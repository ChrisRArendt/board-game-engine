# Copy to .env and fill in from Supabase Dashboard → Settings → API
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: WebRTC TURN (recommended for NATs worldwide). e.g. Metered.ca free tier.
# Leave empty to use STUN only (works on many home networks).
PUBLIC_TURN_URL=
PUBLIC_TURN_USERNAME=
PUBLIC_TURN_CREDENTIAL=
