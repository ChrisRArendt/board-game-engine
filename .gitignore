# Apache basic auth — do not commit
.htpasswd

# Dependencies & build
node_modules
.svelte-kit
build
dist
.output

# Vercel CLI — local project link (not source)
.vercel

# Supabase CLI — local link/cache (not source)
supabase/.temp
.env
.env.*
!.env.example

# Logs & caches
*.log
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# Tests / coverage (if you add a test runner)
coverage

# OS
.DS_Store
Thumbs.db

_misc